# Figma Node 1:26193 — UI Kit

Source file: `FVVplMUExRWDAY1npMxRmj` — Check2Go

Status: **captured from Figma MCP before the read limit was reached**.

This is a key implementation source for the native Android and iOS rebuild.

## Explicit light-theme colors

From frame `1:26194` (`Colors_light`):

- Primary / main button / selected tab text: `#043CB3`
- Primary text: `#002349`
- Inactive tab/icon/text: `#8D919A`
- Main background: `#F4F7FA`

## Explicit dark-theme colors

From frame `1:40768` (`Colors_dark`):

- Primary / main button / selected tab text: `#043CB3`
- Primary text: `#D0D0D0`
- Disabled buttons and surfaces: `#3A4F66`
- Main dark background: `#253C55`
- Inactive tab/icon/text: `#8D919A`

The current product error color is `#B3261E` and is tracked in `docs/design/design-tokens.md`.

## Component area observed

Main component frame: `37:7314` (`Components`).

### Button states
A state list explicitly includes:
- Default
- Hover
- Active
- Disabled

The component group contains four corresponding button variants.

### Header
A reusable `Header` instance is present.

### Bottom / tab navigation
An iPhone `Tab Bar` instance is present and should be treated as the behavioral/design reference for mobile bottom navigation.

### Inputs and choice controls
Observed:
- `input-app` instances;
- radio controls;
- input title/help structure;
- destination-country input example.

### Reminder picker
A compact expanded date/time picker is present with the label:
- “Select date and time for reminder”

### Checklist components
Observed reusable elements include:
- `Checklist` rows;
- `Points-checklist`;
- grouped checklist examples;
- checklist lists followed by a primary button.

### Keyboard / mobile context
An iPhone keyboard instance is included as an interaction/layout reference.

## Example composed UI captured in the kit

One example frame includes:
- heading “Choose your destination country”;
- three app inputs;
- iPhone tab bar.

Another example contains a long vertical list of checklist rows plus a button.

These examples are useful for spacing/composition references, but final implementation should use platform-native layout behavior.

## Older / conceptual color material on the same page

The page also contains an older/conceptual `Colors` section and typography/color analysis blocks with values such as:
- `#FFFFFF`
- `#F7F9FB`
- `#0F172A`
- `#64748B`
- `#94A3B8`
- `#22C55E`
- `#EF4444`
- `#F59E0B`

A separate analysis block also mentions values such as:
- primary blue `#043CB3`;
- hover around `#0A49DC`;
- active around `#032D82`;
- disabled around `#657396`;
- secondary dark around `#264653`;
- background around `#F5F7FB`;
- secondary background / border around `#E9EDF4`;
- primary text around `#0B1F3A`;
- secondary text around `#6B7A90`;
- muted text around `#A8B4C5`.

These blocks are **not authoritative for current implementation** when they conflict with the explicit `Colors_light` / `Colors_dark` frames.

## Typography analysis present in the UI-kit page

The page describes the existing typography as:
- modern sans-serif;
- neutral;
- product-oriented;
- clean;
- readable;
- functional;
- without decorative styling.

Approximate roles described there:
- screen/page title: bold or semibold, ~20–24 px;
- section/card title: semibold, ~16–18 px;
- body: regular, ~14–16 px;
- labels/helper/secondary: regular or medium, ~12–14 px;
- button: medium or semibold, ~14–16 px.

The analysis says this typography supports forms, checklists, cards and navigation and fits a utility-first travel-tech/planner interface.

## Native implementation mapping

Android:
- Kotlin + Jetpack Compose
- `Check2GoTheme`
- `C2GButton`
- `C2GInput`
- `C2GRadio`
- `C2GChecklistItem`
- `C2GTabBar`
- native date/time picker behavior adapted to Android conventions

IOS:
- Swift + SwiftUI
- `Check2GoTheme`
- `C2GButton`
- `C2GInput`
- `C2GRadio`
- `C2GChecklistItem`
- `C2GTabBar`
- native date/time picker behavior adapted to Apple conventions

## Recovery rule

When UI-kit blocks conflict with older visual references or APK colors, the explicit current UI-kit values and direct product-owner decisions take precedence.
